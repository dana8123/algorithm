function solution(s) {
    if(s.length === 1) return 1;

    let min = 1000;
    for(let i = 1; i < s.length/2; i++) {
        let str1= '';
        let str2 = '';
        let ans = '';
        let count = 1;

        for(let j = 0-; j < s.length; j++) {
            if(j === 0) {
                str1 = s.slice(j, j+i);
            }else {
                str2 = s.slice(j, j+i)
                if()
            }
        }
    }
}